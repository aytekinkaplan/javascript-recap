let javaScript = "JavaScript";
let java = "Java";
let c = "C";
let cPlusPlus = "C++";
let cSharp = "C#";
let python = "Python";
let ruby = "Ruby";
let swift = "Swift";

console.log(javaScript, java, c, cPlusPlus, cSharp, python, ruby, swift);

console.log(
  typeof javaScript,
  typeof java,
  typeof c,
  typeof cPlusPlus,
  typeof cSharp,
  typeof python,
  typeof ruby,
  typeof swift
);

console.log(
  typeof javaScript === "string",
  typeof java === "string",
  typeof c === "string",
  typeof cPlusPlus === "string",
  typeof cSharp === "string",
  typeof python === "string",
  typeof ruby === "string",
  typeof swift === "string"
);

console.log(
  javaScript[0],
  java[0],
  c[0],
  cPlusPlus[0],
  cSharp[0],
  python[0],
  ruby[0],
  swift[0]
);

console.log(
  javaScript.length,
  java.length,
  c.length,
  cPlusPlus.length,
  cSharp.length,
  python.length,
  ruby.length,
  swift.length
);

console.log(
  javaScript.toUpperCase(),
  java.toUpperCase(),
  c.toUpperCase(),
  cPlusPlus.toUpperCase(),
  cSharp.toUpperCase(),
  python.toUpperCase(),
  ruby.toUpperCase(),
  swift.toUpperCase()
);

console.log(
  javaScript.toLowerCase(),
  java.toLowerCase(),
  c.toLowerCase(),
  cPlusPlus.toLowerCase(),
  cSharp.toLowerCase(),
  python.toLowerCase(),
  ruby.toLowerCase(),
  swift.toLowerCase()
);

let numOne = 1;
let numTwo = 1;
let numThree = 3;
let numFour = 4;

console.log(numOne, numTwo, numThree, numFour);

console.log(typeof numOne, typeof numTwo, typeof numThree, typeof numFour);

console.log(
  numOne === numTwo,
  numOne === numThree,
  numOne === numFour,
  numTwo === numThree,
  numTwo === numFour,
  numThree === numFour
);

console.log(
  numOne !== numTwo,
  numOne !== numThree,
  numOne !== numFour,
  numTwo !== numThree,
  numTwo !== numFour,
  numThree !== numFour
);

console.log(
  numOne > numTwo,
  numOne > numThree,
  numOne > numFour,
  numTwo > numThree,
  numTwo > numFour,
  numThree > numFour
);

console.log(
  javaScript === java,
  javaScript === c,
  javaScript === cPlusPlus,
  javaScript === cSharp,
  javaScript === python,
  javaScript === ruby,
  javaScript === swift
);

console.log(
  javaScript !== java,
  javaScript !== c,
  javaScript !== cPlusPlus,
  javaScript !== cSharp,
  javaScript !== python,
  javaScript !== ruby,
  javaScript !== swift
);
